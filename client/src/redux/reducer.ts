import {iMovie} from '@/common/type'

const defauleState = {
    data: [],
    isLoading: false,
    condition: {
        page: 1,
        name: '',
        limit: 10
    },
    total: 0
}

const reducer = (state = defauleState, Action) => {
    switch (true) {
        case false:
            break
    }
}